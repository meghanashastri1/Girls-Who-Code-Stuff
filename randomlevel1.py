import random
my_randoms=[]

for i in range (10):
    my_randoms.append(random.randrange(1,101,1))

for elements in my_randoms:
    if elements%5 == 0 
        print (elements)
#this prints a list of 10 random numbers and filters out and prints the multiples of 5 in that list
